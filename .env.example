# ==========================================
# INORG ERP - Environment Configuration
# For Multi-Instance Production Deployment
# ==========================================

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=5000
INSTANCE_ID=instance-1

# Database Configuration (PostgreSQL)
# For multi-instance deployment, add connection pooling parameters:
# ?connection_limit=2&pool_timeout=10&connect_timeout=10
# Example for 100 instances: connection_limit=2 (200 total / 100 instances)
DATABASE_URL="postgresql://username:password@localhost:5432/inorg_db?schema=public"

# Redis Configuration (Required for multi-instance support)
# Used for: session management, caching, Socket.IO adapter
REDIS_URL=redis://localhost:6379

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Origins (comma-separated for multiple domains)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://localhost:3002

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# AI Assistant (TIRAN)
OPENAI_API_KEY=your-openai-api-key-here
TIRAN_MODEL=gpt-4

# GitHub/GitLab Integration
GITHUB_TOKEN=your-github-token
GITLAB_TOKEN=your-gitlab-token

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password

# Frontend URL
CLIENT_URL=http://localhost:3000

# Rate Limiting (requests per minute per user)
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=100

# Session Configuration
SESSION_SECRET=your-session-secret-change-this
SESSION_TTL=86400

# Logging
LOG_LEVEL=info
LOG_TO_FILE=false

# ==========================================
# Multi-Instance Deployment Notes
# ==========================================
# 
# For 100+ instances:
# 1. Set unique PORT and INSTANCE_ID for each instance
# 2. Adjust DATABASE_URL connection_limit based on total instances
# 3. Ensure Redis is running and accessible to all instances
# 4. Use load balancer (Nginx/HAProxy) in front of instances
# 5. See SCALING_GUIDE.md for complete setup instructions
#
# ==========================================

